<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
            android:id="@+id/action_crazypetals"
            android:enabled="true"
            android:icon="@drawable/ic_shopping_cart"
            android:checked="true"
            android:title="@string/menu_crazypetals"
            app:showAsAction="ifRoom" />

    <item
            android:id="@+id/action_exclusive"
            android:enabled="true"
            android:icon="@drawable/selecter_exclusive_icon"
            android:title="@string/menu_exclusive"
            app:showAsAction="ifRoom" />

    <!--<item
            android:id="@+id/action_search"
            android:enabled="true"
            android:icon="@drawable/ic_search"
            android:title="@string/menu_search"
            app:showAsAction="ifRoom" />

    <item
            android:id="@+id/action_profile"
            android:enabled="true"
            android:icon="@drawable/ic_profile_tab"
            android:title="@string/menu_profile"
            app:showAsAction="ifRoom" />-->
</menu>